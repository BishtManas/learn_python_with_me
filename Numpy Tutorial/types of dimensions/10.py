import numpy as np

arr = np.array([1, 2, 3, 4, 5, 6, 7])

print(arr[:4])# this will assume automatically from start and ends with n-1 indexing so in this case [1,2,3,4] will print and this is the output
